const frutas = ["macas", "peras", "uvas", "bananas", "dasmascos", "eduardos"];

// "i " eh um contador, pegamos o tamanho do array, com frutas.length e fizemos
// com que i aumentasse de 1 em 1 ate chegar no maximo do tamanho do array
// para mostrar os nomes, utilizamos o nome do array e o contador 'i', pois ele
// que eh alterado com o for
for (i = 0; i < frutas.length; i++) {
	console.log(frutas[i]);
}
